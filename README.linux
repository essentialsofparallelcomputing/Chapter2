On Ubuntu:

Make sure you have one of the latest versions of GCC

sudo apt-get install openmpi-bin openmpi-common openssh-client openssh-server libopenmpi1.3 libopenmpi-dbg libopenmpi-dev

sudo apt-get install cmake
